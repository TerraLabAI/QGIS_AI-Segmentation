services:
  sam-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - API_KEY=test-key-123
      - SESSION_TTL=600
    # Uncomment for GPU support (requires nvidia-docker):
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
